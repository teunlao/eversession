{
  "name": "eversession",
  "version": "0.1.0",
  "description": "Progressive session compaction for endless Claude Code sessions (multi-agent ready).",
  "type": "module",
  "license": "MIT",
  "private": true,
  "bin": {
    "evs": "dist/cli.js",
    "eversession": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.1.70",
    "@anthropic-ai/tokenizer": "0.0.4",
    "commander": "^14.0.0",
    "node-notifier": "^10.0.1"
  },
  "devDependencies": {
    "@types/node-notifier": "^8.0.5",
    "@types/node": "^24.3.0",
    "@vitest/coverage-v8": "^3.2.4",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
